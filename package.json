{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Full-stack todo application with React frontend and FastAPI backend",
  "scripts": {
    "dev": "./run.sh",
    "backend": "cd backend && source ../.venv/bin/activate && python -m uvicorn main:app --reload --port 8000",
    "frontend": "cd frontend && npm run dev",
    "install-all": "python3 -m venv .venv && source .venv/bin/activate && pip install -r backend/requirements.txt && cd frontend && npm install"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "react-router-dom": "^7.14.2"
  }
}
